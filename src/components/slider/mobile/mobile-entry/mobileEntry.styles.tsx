import styled from "styled-components/macro";

export const EntryItemWrapper = styled.div`
  position: relative;
  border-radius: 1em;
  overflow: hidden;

  img {
    height: 12em;
    object-fit: cover;
  }
`;
