import styled from "styled-components/macro";

export const ItemWrapper = styled.div`
  height: 16em;
  position: relative;
  border-radius: 1em;
  overflow: hidden;

  img {
    height: 100%;
    object-fit: cover;
  }
`;
