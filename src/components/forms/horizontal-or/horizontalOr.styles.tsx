import styled from "styled-components/macro";

export const StyledOr = styled.div`
  display: flex;
  align-items: center;
  div {
    flex-grow: 1;
  }

  p {
    font-size: 1rem;
    color: hsl(0, 0%, 0%, 0.5);
    padding-inline: 0.5em;
  }
`;
